<Project>
  <!-- Everything lives in the repo-root props; this just chains to it so the demos and the
       library share one set of defaults. -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
